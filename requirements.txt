fastapi==0.115.6
uvicorn[standard]==0.34.0
playwright==1.49.1
httpx==0.28.1
open_clip_torch==2.29.0
scikit-learn==1.6.0
Pillow==11.0.0
numpy==2.2.1
aiosqlite==0.20.0
python-multipart==0.0.20

# torch/torchvision MUST be installed separately from the CPU-only index first,
# otherwise pip will pull the default CUDA build (adds ~4GB of unused nvidia/cuda
# packages) to satisfy open_clip_torch's unpinned torch/torchvision dependency.
# This machine has no GPU, so always run this before `pip install -r requirements.txt`:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.13.0 torchvision==0.28.0
